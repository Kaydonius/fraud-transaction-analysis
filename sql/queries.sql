-- include queries for total num tranactions, draud rate by day,
-- top 10 suspicious users, avg transactions, highest risk merchants/categories

-- test query
SELECT COUNT (*) AS total_transactions
FROM transaction_data;